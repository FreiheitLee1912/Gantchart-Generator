# Gantt Chart Generator v3 業務向け説明書

## 目的

Gantt Chart Generator v3 は、Jira から出力した CSV ファイルを読み込み、プロジェクト報告や会議資料に使いやすいガントチャートを作成するためのローカルツールです。

Jira のデータをそのまま PowerPoint に貼り付けるのではなく、業務報告で見やすいタイムライン形式に整え、PPTX として出力できます。

## v3 でできること

- Jira CSV からガントチャートを自動作成
- ブラウザ上でタイムラインを確認
- ブラウザ上で開始日・終了日を直接編集
- 16:9 形式の PowerPoint を出力
- 4:3 形式の PowerPoint を出力
- 修正しやすい Simple CSV を出力
- 修正した Simple CSV を再アップロードして再生成

## 主な利用シーン

- プロジェクト進捗報告
- 定例会議・月次報告資料の作成
- マイルストーンの共有
- Step Meeting の日程確認
- 関係部門とのスケジュール調整
- Jira データをもとにした PowerPoint 資料の短時間作成

## 入力ファイル

v3 では、Jira から出力した `.csv` ファイルを使用します。

主に以下の項目を読み取ります。

| 業務上の意味 | CSV 上の項目 |
|---|---|
| タイムライン分類 | Grouping |
| 課題タイプ | Issue Type |
| タスク名 | Summary |
| ステータス | Status |
| 開始日 | Start Date / Target Start |
| 終了日 | Due Date / End Date / Target End |
| 課題キー | Issue Key |

開始日・終了日のどちらも入っていない行は、ガントチャートには表示されません。

## 基本的な使い方

1. `OPEN_GANTT_GENERATOR_V3.html` または `index_v3.html` を開きます。
2. Jira CSV ファイルをアップロードします。
3. ブラウザ上でタイムラインを確認します。
4. 必要に応じて、画面左側の `Start Date` / `End Date` を直接編集します。
5. 必要に応じて `PPT range` で出力期間を調整します。
6. `Export 16:9 PPTX` または `Export 4:3 PPTX` をクリックします。
7. CSV 上でまとめて修正したい場合は `Download Simple CSV` をクリックします。
8. Simple CSV を修正し、再度アップロードしてガントチャートを再生成します。

## 日付の修正方法

v3 では、日付の修正方法が 2 種類あります。

### 1. Web 画面で直接修正する

アップロード後の一覧画面では、左側に `Type`、`Timeline`、`Start Date`、`End Date`、`Status` が表示されます。

`Start Date` と `End Date` は直接編集できます。日付を変更すると、画面上のガントチャート位置も更新されます。その状態で PowerPoint を出力すると、修正後の日付が反映されます。

入力できる形式は以下です。

```text
26/6/15
26-06-15
```

画面上の日付表示は `yy/m/d` 形式です。

### 2. Simple CSV でまとめて修正する

複数行をまとめて修正したい場合や、Excel で確認しながら修正したい場合は、`Download Simple CSV` を使用します。

Simple CSV を修正して再アップロードすると、修正後の内容でガントチャートを再生成できます。

## Simple CSV について

Jira の CSV は列数が多く、修正したい行や列を探すのに時間がかかる場合があります。

v3 の `Download Simple CSV` を使うと、修正に必要な項目だけを抜き出した簡易 CSV を出力できます。

Simple CSV の列は以下です。

```text
Summary, Issue Key, Grouping, Issue Type, Status, Start Date, End Date
```

このファイルを使うことで、タスク名、分類、開始日、終了日などを簡単に修正できます。

修正後の Simple CSV は、そのまま v3 に再アップロードできます。

Simple CSV 内の日付は、曜日付きで以下の形式になります。

```text
26/6/15（Mon）
26/6/20（Sat）
```

この形式のまま再アップロードできます。

## PPT 出力の見え方

v3 の PowerPoint 出力では、以下のような表示になります。

- 左側の列名は `Timeline` と表示されます。
- Timeline の分類名では、`04.` や `05.` などの番号は表示されません。
- ただし、並び順は元の番号を使って維持されます。
- Milestone は赤色・太字で強調されます。
- Step Meeting は菱形マークで表示され、文字は重ならないように配置されます。
- 月ごとの境界には薄いグレーの縦線が入ります。
- 出力期間内に本日が含まれる場合、赤い Today 線が表示されます。

## v3 の対象ファイル

この説明書は v3 のみを対象としています。

- `index_v3.html`
- `app_v3.js`
- `OPEN_GANTT_GENERATOR_V3.html`

v1 や v2 の仕様は、この説明書には含みません。

## 注意事項

- このツールはローカル HTML ツールです。サーバー起動は不要です。
- `vendor` フォルダは削除しないでください。PPTX 出力に必要です。
- 日付の正確性は、アップロードした CSV の内容に依存します。
- 日付が入っていないタスクは表示対象外です。
- 正式なデータ管理は Jira 側で行い、v3 は資料作成・確認用として利用してください。

## 業務上のメリット

v3 を使うことで、Jira CSV から業務報告用のガントチャート PPT を短時間で作成できます。

また、Simple CSV を使うことで、修正と再出力のサイクルを簡単に回せます。

これにより、手作業で PowerPoint のガントチャートを修正する時間を削減し、会議資料作成の効率を高めることができます。
